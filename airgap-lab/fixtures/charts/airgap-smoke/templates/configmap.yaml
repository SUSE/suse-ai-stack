apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "airgap-smoke.fullname" . }}
  labels:
    {{- include "airgap-smoke.labels" . | nindent 4 }}
data:
  result: "pulled and deployed without public registry access"
