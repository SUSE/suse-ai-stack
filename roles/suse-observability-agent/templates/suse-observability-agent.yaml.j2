global:
  customCertificates:
    enabled: true
    configMapName: "{{ suse_observability_cm_name_tls }}"
stackstate:
  apiKey: "{{ suse_observability_api_token }}"
  cluster:
    name: "{{ stackstate_cluster_name }}"
  url: "{{ stackstate_url }}"
nodeAgent:
  skipKubeletTLSVerify: true
